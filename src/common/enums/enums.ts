export enum TasksStatuses {
  New,
  InProgress,
  Completed,
  Draft,
}

export enum TaskPriorities {
  Low,
  Middle,
  Hi,
  Urgently,
  Later,
}

export enum RESULT_CODE {
  SUCCEEDED = 0,
  ERROR = 1,
  CAPTCHA = 10,
}
